# Core runtime has NO hard dependencies — the app runs on the Python stdlib.
# These are OPTIONAL and improve performance / unlock providers.

# Faster vector math (recommended)
numpy>=1.24

# PDF parsing (required only to ingest .pdf files)
pypdf>=3.17

# Advanced PDF ingestion: layout-aware text (PyMuPDF sort=True) + table detection.
# Used by ingestion/advanced_loader.py; falls back to pypdf if absent.
pymupdf>=1.24
pdfplumber>=0.11

# Local neural embeddings (default embedder). Falls back to LocalEmbedder if absent.
sentence-transformers>=2.7

# Graph algorithms for personalized-PageRank retrieval (retrieval.graph_retriever=ppr)
networkx>=3.0

# Faster HTTP (optional; stdlib urllib is used if absent)
requests>=2.31

# Web crawling: robust HTML + table extraction (ingestion/web_extract.py).
# Falls back to a stdlib regex extractor if absent.
beautifulsoup4>=4.12
lxml>=5.0

# Headless-browser rendering for JS/bot-protected sites (ingestion/browser.py).
# OPTIONAL: render="auto"/"always" falls back to static fetch if not installed.
# Enable with:  pip install playwright && playwright install chromium
# playwright>=1.44

# --- Optional production API server (instead of the bundled stdlib server) ---
# fastapi>=0.110
# uvicorn>=0.29

# --- Optional providers ---
# neo4j>=5.0           # graph_store.provider = neo4j
# pytesseract>=0.3     # OCR (also needs the tesseract binary)
# pillow>=10.0         # image loading for OCR/vision

# Optional: tighter Leiden communities (falls back to networkx Louvain if absent)
leidenalg
igraph
